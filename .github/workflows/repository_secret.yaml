name: variable & secrets
on: workflow_dispatch

jobs:
  repository-secret:
    runs-on: ubuntu-latest
    env:
      REPOSITORY_SECRET: ${{secrets.REPOSITORY_SECRET}}
    steps:
      - name: show secret
        run: echo "${#REPOSITORY_SECRET}"