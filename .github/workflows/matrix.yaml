name: matrix
on: workflow_dispatch

jobs:
  matrix:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.10, 3.11, 3.12]
    steps:
      - name: show python version
        run: python --version